import Typography from '@suid/material/Typography';

export default function CloseThisWindow() {
    return (
        <>
            <Typography variant="body1" component="div" gutterBottom>
                You can close this window
            </Typography>
        </>
    );
}
